import { ToCenterDistancePipe } from './to-center-distance.pipe';

describe('ToCenterDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new ToCenterDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
